{
  "scripts": {
    "build:ts": "tsc -p tsconfig.json",
    "build:assets": "cpy src/public/*.{html,css} --flat dist/public && cpy src/server.js --flat dist",
    "build": "npm run build:ts && npm run build:assets"
  },
  "type": "module",
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/jwt": "^9.1.0",
    "@fastify/static": "^8.1.1",
    "@fastify/websocket": "^11.0.2",
    "bcrypt": "^5.1.1",
    "fastify": "^5.3.2",
    "jsonwebtoken": "^9.0.2",
    "socket.io": "^4.7.2",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "cpy-cli": "^5.0.0"
  }
}
